import 'package:flutter/material.dart';

class SubmitNewPost extends StatelessWidget {
  const SubmitNewPost({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
